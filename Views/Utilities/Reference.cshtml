@{
    ViewData["Title"] = "Home Page";
}

    <div class="full-screen-background">
        <div class="content text-center">
        <h1 class="display-4"><i class="fas fa-link"></i> Reference DVA in connectors</h1>
        
        </div>

    <div class="container mt-5">
        <!-- Añade 'table-responsive' para hacer la tabla responsiva -->
        <div class="table-responsive">
            <!-- Barra de búsqueda -->
            <form class="row g-2">
                <div class="col-auto">
                    <label for="partNumber" class="visually-hidden"> Codigo</label>
                    <input type="text" class="form-control" id="partNumber" placeholder="Codigo">
                </div>
                <div class="col-auto">
                    <button type="submit" class="btn btn-primary mb-3"><i class="fas fa-search"></i> Buscar</button>
                </div>
            </form>
            <table class="table">
                <thead class="thead-dark">
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Componente</th>
                        <th scope="col">Diseño</th>
                        <th scope="col">Estación</th>
                        <th scope="col">Art. Mec.</th>
                        <th scope="col">Coordenada</th>
                        <th scope="col">Grupo</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Los datos de la tabla irían aquí -->
                    <tr>
                        <th scope="row">1</th>
                        <td>Componente 1</td>
                        <td>Diseño 1</td>
                        <td>Estación 1</td>
                        <td>Art. Mec. 1</td>
                        <td>Coordenada 1</td>
                        <td>Grupo 1</td>
                    </tr>
                    <!-- Repite las filas según sea necesario -->
                </tbody>
            </table>
        </div>
    </div>

    <!-- Enlaza los archivos JS de Bootstrap y sus dependencias -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.9.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    </div>
